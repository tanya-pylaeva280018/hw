palindrome = 0
for i in range(100, 1000):
    for j in range(100, 1000):
        num, strNum = i * j, str(i * j)
        if (strNum == strNum[::-1]) and num > palindrome:
            palindrome = num
print (palindrome)
